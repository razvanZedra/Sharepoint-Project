import * as ts from 'typescript';
export declare class TypeScriptMessageFormatter {
    /**
     * Format a TypeScript diagnostic message or message chain.
     */
    static format(messageText: string | ts.DiagnosticMessageChain): string;
}
